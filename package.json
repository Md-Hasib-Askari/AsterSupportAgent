{
  "name": "aster-support-agent",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "bunx turbo run build",
    "dev": "bunx turbo run dev",
    "lint": "bunx turbo run lint",
    "clean": "bunx turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "bun@1.3.10"
}
